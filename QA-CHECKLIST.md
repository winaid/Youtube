# 출시 직전 수동 QA 체크리스트

> 최종 점검일: 2026-03-20
> 대상: AI 영상 제작 스튜디오 (VEO 전용)

---

## A. 배포 전 환경 확인

- [ ] Cloudflare Pages에 `GEMINI_API_KEY` 환경 변수가 설정되어 있는가
- [ ] Cloudflare Pages에 `GEMINI_API_KEY_2` (폴백용) 환경 변수가 설정되어 있는가
- [ ] R2 버킷 바인딩(`VIDEO_BUCKET`)이 Pages Functions에 연결되어 있는가
- [ ] `npm run build` 가 에러 없이 완료되는가 (warning은 허용)
- [ ] 배포된 URL에서 `/api/generate-video` 엔드포인트가 200/405 응답을 반환하는가 (404 아닌지 확인)
- [ ] 응답 헤더에 `Cross-Origin-Opener-Policy: same-origin` 이 포함되는가 (FFmpeg.wasm용)
- [ ] 모바일(9:16 세로) / 데스크톱(16:9 가로) 해상도에서 페이지가 로드되는가

---

## B. 핵심 사용자 플로우 점검

### B-1. 8초 멀티샷 생성 (최우선)

- [ ] 스토리 텍스트 입력 → "컷 생성" 클릭 → `/api/generate-cuts` 호출 성공
- [ ] 생성된 컷 카드에 4개 샷 구조(establish → develop → peak → resolve)가 표시되는가
- [ ] 영상 생성 설정에서 duration이 `8s`로 고정 표시되는가
- [ ] "영상 생성" 클릭 → `/api/generate-video` POST body에 `durationSeconds: 8` 확인
- [ ] 생성 중 폴링 상태가 "대기 중" → "요청을 보냈어요" → "영상을 만들고 있어요" → "완료!" 순서로 진행
- [ ] 완료 후 영상 프리뷰가 정상 재생되는가
- [ ] 영상 생성 후 R2 업로드(`/api/upload-video`)가 자동 실행되는가

### B-2. 8→15→22초 Extend 체인 (최우선)

- [ ] 컷 1 완료 후 컷 2 생성 시 `videoMode: "extend"` 로 전송되는가
- [ ] 컷 2의 `sourceVideo` 필드에 컷 1의 `canonicalVideoUri`가 포함되는가
- [ ] 컷 2 완료 → 누적 재생 시간이 약 15초인가 (8 + 7)
- [ ] 컷 3 완료 → 누적 재생 시간이 약 22초인가 (15 + 7)
- [ ] 연속 재생 시 컷 간 시각적 연속성(인물, 배경, 색감)이 유지되는가
- [ ] sourceVideo가 없는 경우 text-to-video 폴백이 작동하는가 (콘솔에 degradation 로그)

### B-3. Reference Image 동작 (현재 비활성)

> **현재 정책:** VEO API가 reference image를 지원하지 않으므로 UI 비활성 상태.
> 아래 항목은 향후 API 지원 시 재활성화 대상이며, 현재는 점검 대상이 아닙니다.

- [x] ~~영상 설정 패널에서 참조 이미지 영역이 `opacity-50 pointer-events-none`으로 비활성화~~
- [x] ~~서버에서 `referenceImages` 필드 수신 시 무시 (호환용 수신만)~~

### B-4. 히스토리 / 새로고침 후 이어서 작업 (최우선)

- [ ] 영상 생성 완료 후 `localStorage["video-jobs"]` 에 레코드가 저장되는가
- [ ] 영상 완료 후 `localStorage["video-history"]` 에 이력이 추가되는가
- [ ] 생성 중(processing 상태) 페이지 새로고침 → 자동으로 폴링이 재개되는가
- [ ] 새로고침 후 이전 컷의 canonicalVideoUri를 이용해 extend 체인을 이어갈 수 있는가
- [ ] 프롬프트 히스토리(`localStorage["prompt-generation-history"]`)에서 이전 입력 복원 가능
- [ ] 시리즈 프로젝트(`localStorage["yt-series-projects"]`)가 새로고침 후 유지되는가
- [ ] 브라우저 탭 닫았다 다시 열어도 히스토리 패널에 이전 영상 목록이 표시되는가

---

## C. 에러 / 엣지케이스 점검

### 네트워크 / API

- [ ] 네트워크 끊김 시 "시간이 오래 걸리고 있어요" 상태로 전환 후 재연결 시 재개
- [ ] VEO API 5xx 에러 → 지수 백오프 재시도(2s → 5s → 10s → 20s) 동작 확인
- [ ] VEO API 4xx 에러 → "생성에 실패했어요" 표시, 재시도 없음
- [ ] 폴링 90회 초과 시 타임아웃 처리 후 `timeout_recoverable` 상태 전환
- [ ] RAI 필터링된 프롬프트 → 생성 차단 + 사용자에게 수정 안내 표시

### 입력 엣지케이스

- [ ] 빈 스토리 텍스트로 "컷 생성" 클릭 → 적절한 유효성 검증 메시지
- [ ] 매우 긴 스토리(3000자 이상) 입력 → 프롬프트 2500자 제한 내 자동 축약
- [ ] 특수문자/이모지 포함 스토리 → 정상 처리
- [ ] 동시에 여러 컷 생성 요청 → 순서대로 큐 처리, 중복 실행 없음

### 스토리지

- [ ] localStorage 용량 초과 시 자동 정리(오래된 레코드 삭제) 후 저장 재시도
- [ ] 7일 경과 완료/실패 job 레코드가 자동 삭제되는가
- [ ] 히스토리 200건 초과 시 oldest-first 삭제 동작 확인

---

## D. 성능 / UX 점검

### 로딩 & 반응성

- [ ] 초기 페이지 로드 시간 < 3초 (Lighthouse 기준)
- [ ] 컷 생성 요청 후 UI 피드백(로딩 스피너/프로그레스)이 즉시 표시되는가
- [ ] 영상 생성 중 다른 UI 조작(설정 변경, 스크롤)이 블로킹 없이 가능한가
- [ ] 10개 이상 컷이 있는 긴 시퀀스에서 타임라인 스크롤이 부드러운가

### 사용자 경험

- [ ] 모든 한국어 UI 텍스트가 깨지지 않고 올바르게 표시되는가
- [ ] 영상 설정 패널 접기/펼치기 토글이 정상 동작하는가
- [ ] 영상 프리뷰 플레이어에서 재생/일시정지/음량 조절 가능한가
- [ ] 다크모드가 있다면 모든 컴포넌트에서 정상 렌더링되는가
- [ ] 모바일(터치) 환경에서 버튼 탭, 스크롤, 입력이 정상 동작하는가

---

## E. 출시 직후 모니터링 항목

### 첫 24시간

- [ ] Cloudflare Pages 대시보드에서 5xx 에러율 < 1% 유지
- [ ] `/api/generate-video` 평균 응답 시간 < 5초 (submit 단계)
- [ ] R2 스토리지 사용량 급증 여부 모니터링 (비정상 대량 생성 감지)
- [ ] `GEMINI_API_KEY` 할당량 소진 경고 확인 (Google Cloud Console)

### 첫 1주일

- [ ] 사용자 생성 성공률 > 90% (실패 job 비율 추적)
- [ ] extend 체인 평균 길이 확인 (3컷 이상 도달 비율)
- [ ] ~~reference image~~ (현재 비활성 — 향후 API 지원 시 재추가)
- [ ] localStorage 관련 클라이언트 에러 보고 유무
- [ ] VEO API 모델별(3.1 fast / 3.1 standard) 성공률 비교
- [ ] 사용자 세션 평균 체류 시간 및 이탈 지점 파악
